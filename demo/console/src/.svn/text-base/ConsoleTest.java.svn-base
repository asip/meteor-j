/*
 *
 *
 */

import jp.kuro.meteor.*;

public class ConsoleTest {
    public static void main(String args[]) {

        try{

        ParserFactory pf = ParserFactory.build(Parser.XML, "d:\\kuro\\meteor\\demo\\console\\src\\test.xml", "Shift_JIS");
        Parser xt = pf.getParser();

        Element tagX = xt.findElement("momo");
        xt.setContent(tagX, "<>");
        //Tag tag  = xt.searchTag("test","manbo","manbo");

        Element tag = xt.findElement("manbo", "manbo");
        //Element tag = null;

        //Tag tag2 = xt.searchTag("test","manbo","mango");
        Element tag2 = xt.findElement("manbo", "mango");
        //System.out.println(xt.getAttributeValue(tag,"manbo"));
        Element tagY = xt.findElement("kobe", "momo", "mono");
        xt.setElementName(tagY, "mop");
        xt.setElementName(tagY, "kobeto");
        xt.setAttribute(tagY, "momo", "pono");
        xt.setAttribute(tagY, "momo", "pino");

        //System.out.println(xt.getAttributeValue(tagY,"momo"));
        //xt.setAttribute(tag,"manbo","mango");
        xt.setAttribute(tag2, "manbo", "okojo");
        tag  = xt.findElement("test","manbo","mbo");
        //xt.setContent(tag,"<>\"'&\"");
        //System.out.println(xt.getContent(tag));
        //xt.execute(tag, (new TestHook()));

        Parser xt2 = xt.getChildParser(tag);

        Element tag_= xt2.findElement("tech","mono","mono");

        for(int i=0;i<5;i++){
            xt2.setAttribute("test","bb" + i);
            xt2.setContent(tag_,"konkon" + i);
            xt2.print();
        }

        xt2.flush();

        tag_= xt.findElement("tech","mono","mono2");
        xt2 = xt.getChildParser(tag_);

        for(int i=0;i<5;i++){
            xt2.setContent("konkon" + i);
            xt2.print();
        }

        xt2.flush();

        
        //xt.setElementToQuarkTag("mono","<>\"'&mono");
        Element tag3 = xt.findCXTag("mono");
        xt.setContent(tag3, "<>\"&mono");
        //xt.eraseQuarkTag(tag3);
        //xt.eraseQuarkTag("mono");
        tag = xt.findElement("teo", "a", "aa");
        //xt.eraseAttribute(tag,"b");
        //tag = xt.findElement("mink");

        //Element tag4 = xt.findCXTag("jj");
        //xt.execute(tag4,(new TestHook()));



        Element tag5 = xt.findElement("potato", "id", "aa");

        //Element tag6 = xt.findElement("potato", "id", "bb");

        //System.out.println(tag5.getName());
        //System.out.println(tag5.getAttributes());
        //System.out.println(tag5.getContent());

        //xt.getAttributeMap(tag5);

        xt.setAttribute(tag5,"id","bb");
        //xt.setContent(tag5,"cc");
        xt.setAttribute(tag5,"id","cc");

        Element tag7 = xt.findElement("potato","id","aa","id2","bb");

        Element tag8 = xt.findElement("hamachi","id","aa","id2","bb");

        System.out.println(tag5.getAttributes());
        
        System.out.println(xt.getAttributeValue(tag7,"lc"));

        System.out.println(tag8.getAttributes());

        //xt.setTagName(tag,"teoo");
        xt.print();

        System.out.println(xt.getDocument());

        }catch(Exception e){
            e.printStackTrace();    
        }
    }
}